import styled from 'styled-components'

export const InputComponent = styled.input`
  border: none;
  padding-left: 4px;
  border-bottom: solid 3px var(--primary-bd);
  border-radius: 0;
  font-size: min(26px, 6vw);
  padding-bottom: 10px;
`
